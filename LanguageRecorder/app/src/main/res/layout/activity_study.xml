<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="com.example.lachlan.myfirstapp.StudyActivity">

    <ListView
        android:listSelector="@drawable/list_item_selector"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/study_list"
        android:layout_above="@+id/nextbutton" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Next"
        android:id="@+id/nextbutton"
        android:layout_alignParentBottom="true"
        android:layout_alignRight="@+id/study_list"
        android:layout_alignEnd="@+id/study_list"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:background="#00A887"
        android:clickable="true"
        android:enabled="true"
        android:onClick="loadMunicipalityActivity" />

</RelativeLayout>
